from django.apps import AppConfig


class AlicuotaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'alicuota'
    verbose_name = 'Configuracion'  # Cambia "Gestión de Vehículos" a lo que prefieras


